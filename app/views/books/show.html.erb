<h1>Book detail</h1>

   <% @books.each do |book| %>
   
     <%= attachment_image_tag @user, :image, :fill, 100, 100, format: 'jpeg', fallback: "no-image.jpeg" %>
     <%= book.title %>
     <%= book.opinion%>
   
   <% end %>



<%= render 'users/info', user: @user %>

<%= render 'books/new', book: @book_new %>